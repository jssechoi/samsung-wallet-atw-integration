# Demo React / Next.js Frontend (Phase 3 – Placeholder)

This directory is reserved for a **modern web frontend** demo (React or Next.js with TypeScript) that will:

- Consume the same partner APIs (transit-data, fetch-data, update-notification).
- Provide a reusable “Add to Samsung Wallet” button component.
- Handle CORS and deployment considerations when the frontend is served from a different origin than the backend.

**Status:** Not yet implemented. See the root [README](../README.md) for the overall roadmap.
